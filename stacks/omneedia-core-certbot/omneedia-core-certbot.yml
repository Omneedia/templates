template:
  robocert:
    image: adferrand/dnsrobocert
    environment:
      ROOT: /etc/dnsrobocert
      VERSION: latest
    volumes:
      - ${ROOT_CERTS}:/etc/letsencrypt
      - ${ROOT_OMNEEDIA}/certs:/etc/dnsrobocert
